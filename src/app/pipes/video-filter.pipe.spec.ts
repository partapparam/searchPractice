import { VideoFilterPipe } from './video-filter.pipe';

describe('VideoFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new VideoFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
